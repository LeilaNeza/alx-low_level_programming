#include <stdio.h>


/**
 * main - program that prints single digit numbers
 *
 * Return: Always 0.
 */

int main(void)
{
	int number;

	for (nmber = 0; number < 10; number++)

	{
		putchar(number);
	}

	putchar('\n');

	return (0);
}
